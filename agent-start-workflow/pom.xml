<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.aigoodle</groupId>
        <artifactId>agent-start</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>agent-start-workflow</artifactId>
    <name>agent-start-workflow</name>
    <description>Workflow / agent orchestration: a DAG engine with a variable pool and real nodes (LLM, agent,
        knowledge retrieval, condition, HTTP, template, classifier, iteration). Depends on agent-start-model;
        knowledge retrieval is optional.</description>

    <dependencies>
        <dependency>
            <groupId>io.github.aigoodle</groupId>
            <artifactId>agent-start-model</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.aigoodle</groupId>
            <artifactId>agent-start-agent</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.aigoodle</groupId>
            <artifactId>agent-start-knowledge</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.github.aigoodle</groupId>
            <artifactId>agent-start-tools</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Optional: powers the CODE node. Applications pull it in explicitly to enable
             embedded JEXL expressions/scripts; without it the CODE executor is not registered. -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-jexl3</artifactId>
            <version>3.3</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test infra -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
